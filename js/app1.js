/*window.addEventListener('load', funciton(){
	var foods = require ('./cars');

	var generator = _.template(document.getElementById('music-template').textContent);
		for (var i = 0; i < music.length; i++){
			var html = generator ({
				music: musics[i],
});

		var yoyo = document.createElement('div');
		yoyo.classList.add('music');
		yoyo.setAttribute('id', 'music-' + musics[i].id);
		el.innerHTML = html;

		yoyo.appendChild(yoyo);

	}

	var search = document.getElementById('search-box');
	search.addEventListener('keyup', function(){
		var pattern = new RegExp(search.value);
		var word = search.value;
		for (var i = 0; i < musics.length; i ++) {
			var element = document.getElementById('music-' + musics[i].id);
			if (musics[i].name === word ){

			 element.classList.remove('hidden');
		}else{
			 element.classList.add('hidden');
			}
		}

	});

});
*/
